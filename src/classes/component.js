class component {
	/**
	 * Class component that represents default methods and values set by the MonkeySet components core
	 *
	 * @summary Initializes default methods and values representing a valid MonkeySet Component
	 * @ignore
	 * @constructor
	 */
	constructor(monkeyset) {
		this.monkeyset = monkeyset
	}
}

module.exports = component
